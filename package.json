{
  "name": "@roelofr/client",
  "license": "AFL-3.0",
  "repository": "git@github.com:roelofr/se2-client.git",
  "scripts": {
    "lint": "yarn --cwd client run lint",
    "build": "yarn --cwd client run build",
    "start": "yarn --cwd client run start",
    "watch": "yarn --cwd client run watch",
    "postinstall": "yarn --cwd client install"
  }
}
